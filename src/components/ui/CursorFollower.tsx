"use client";

// CursorFollower removed - too excessive for a clean portfolio.
// Keeping the component file to avoid import errors, but rendering nothing.
export default function CursorFollower() {
  return null;
}
